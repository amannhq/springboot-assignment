# ============================================
# BASE CONFIGURATION
# ============================================
# This file contains shared/default settings
# Profile-specific settings override these values

# Default active profile (use -Dspring.profiles.active=prod for production)
spring.profiles.active=${SPRING_PROFILES_ACTIVE:dev}

# Application name
spring.application.name=rideshare-api

